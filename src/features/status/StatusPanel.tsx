import type { Player } from "../../types/domain";
import { CLASSES } from "../../game/classes"; import { RACES } from "../../game/races";
export function StatusPanel({ player }: { player: Player }) { const race = RACES.find((item) => item.id === player.raceId)?.name ?? "—"; const gameClass = CLASSES.find((item) => item.id === player.classId)?.name ?? "—"; return <section className="module-panel"><header><p className="kicker">IDENTITY RECORD</p><h1>{player.displayName}</h1><p>{player.playerId} // LEVEL {player.level}</p></header><div className="status-columns"><dl><div><dt>RACE</dt><dd>{race}</dd></div><div><dt>CLASS</dt><dd>{gameClass}</dd></div><div><dt>CONDITION</dt><dd>{player.condition}</dd></div><div><dt>REGION</dt><dd>{player.regionId ?? "Unknown"}</dd></div></dl><dl className="stat-list">{Object.entries(player.stats).map(([key, value]) => <div key={key}><dt>{key.toUpperCase()}</dt><dd>{value}</dd></div>)}</dl></div></section>; }
