import type { Player } from "../../types/domain"; import { EmptyState } from "../../components/ui/EmptyState"; import { StatusPanel } from "../status/StatusPanel"; import { WorldView } from "./WorldView"; import type { Module } from "../../system/navigation/SystemNavigation";
const COPY: Record<Exclude<Module, "WORLD" | "STATUS">, [string, string]> = { INVENTORY: ["INVENTORY EMPTY", "Itens adquiridos serão materializados aqui."], EQUIPMENT: ["NO EQUIPMENT LINKED", "Equipamentos pertencentes ao jogador aparecerão aqui."], SKILLS: ["NO SKILLS REGISTERED", "O Sistema ainda não reconheceu nenhuma habilidade."], QUESTS: ["NO ACTIVE DIRECTIVES", "Nenhuma missão foi atribuída."], PARTY: ["PARTY CHANNEL IDLE", "Você não está conectado a uma party."], GUILD: ["NO GUILD LINK", "Explore ou solicite uma guilda quando o módulo estiver disponível."], TITLES: ["NO TITLES UNLOCKED", "Títulos conquistados serão registrados aqui."], SYSTEM: ["SYSTEM CONTROL", "Preferências e encerramento de sessão ficam no controle central."] };
export function ModuleView({ module, player }: { module: Module; player: Player }) { if (module === "WORLD") return <WorldView player={player}/>; if (module === "STATUS") return <StatusPanel player={player}/>; const [title, message] = COPY[module]; return <EmptyState code={`${module} // READY`} title={title} message={message}/>; }
