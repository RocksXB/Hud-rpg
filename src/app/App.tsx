import { useCallback, useState } from "react"; import { AuthProvider, useAuth } from "./providers/AuthProvider"; import { ErrorBoundary } from "./ErrorBoundary"; import { LinkStart } from "../features/auth/LinkStart"; import { AuthScreen } from "../features/auth/AuthScreen"; import { CharacterCreation } from "../features/character/CharacterCreation"; import { SystemShell } from "../system/shell/SystemShell";
function SystemGateway() { const [intro, setIntro] = useState(true); const completeIntro = useCallback(() => setIntro(false), []); const session = useAuth(); if (intro) return <LinkStart onComplete={completeIntro}/>; if (session.status === "loading") return <main className="loading" aria-live="polite"><p className="kicker">RESTORING SYSTEM LINK</p></main>; if (session.status === "error") return <main className="fatal" role="alert"><h1>RECOVERY REQUIRED</h1><p>{session.error}</p><button onClick={() => void session.reload()}>RETRY</button></main>; if (!session.player) return <AuthScreen/>; if (!session.player.characterCreated) return <CharacterCreation player={session.player} onComplete={session.reload}/>; return <SystemShell player={session.player}/>; }
export function App() { return <ErrorBoundary><AuthProvider><SystemGateway/></AuthProvider></ErrorBoundary>; }
